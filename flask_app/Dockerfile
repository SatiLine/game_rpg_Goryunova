FROM python:3.11-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml .
RUN uv sync --no-dev

COPY src/ src/
COPY alembic/ alembic/
COPY alembic.ini .

CMD ["sh", "-c", "uv run alembic upgrade head && uv run python -m flask --app src.app:create_app run --host 0.0.0.0 --port 5000"]